"""
This module has been intentionally left empty.
Sample data generation was removed as per user request.

For real-world usage, candidates should be uploaded through the resume API endpoints:
- POST /resume/upload/{job_id}: Upload a candidate resume
- POST /resume/process: Process resumes for a job
"""
